<!DOCTYPE html>
<html lang="en">

<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Form Submission</title>
        <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-orange-200 flex items-center justify-center min-h-screen">

        <form action="/" method="POST" class="bg-white/60 shadow-lg rounded-xl p-6 w-96 space-y-3">
                <% if (submitted) { %>
                        <p class="text-green-700 text-xl font-semibold text-center animate-pulse">
                                Thanks <%= name %>! We’ve received your message.
                        </p>
                        <p class="text-green-600 text-center mt-3">Your message was <br> <strong>
                                        <%= message %>
                                </strong>
                        </p>
                        <p class="text-green-600 text-center mt-3">Form submitted by <strong>
                                        <%= name %>
                                </strong> (<%= email %>)
                        </p>


                        <% } else {%>

                                <h2 class="text-4xl font-bold text-center mb-4">User Form</h2>

                                <input type="text" name="name" placeholder="Name"
                                        class="w-full border rounded-xl px-4 py-2" required>
                                <input type="email" name="email" placeholder="Email"
                                        class="w-full border rounded-xl px-4 py-2" required>
                                <textarea name="message" placeholder="Enter message" cols="30" rows="4"
                                        class="w-full border rounded-xl px-4 py-2 resize-none" required></textarea>

                                <button type="submit"
                                        class="w-full bg-blue-600 hover:bg-blue-700 text-white rounded p-2 transition">
                                        Submit
                                </button>
                                <% } %>

        </form>

</body>

</html>